import React from'react';import{useAuth}from'../features/auth/AuthContext';export const navItems=[['dashboard','⌂','Tổng quan'],['users','♙','Khách hàng'],['products','▦','Sản phẩm'],['orders','▤','Đơn hàng'],['feedback','♡','Phản hồi'],['surveys','☑','Khảo sát'],['reports','◒','Báo cáo']];export default function AdminLayout({active,onNavigate,children}){const{user,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">✦</span><span>NovaCRM</span></div><div className="workspace-label">WORKSPACE</div><nav>{navItems.map(([id,icon,label])=><button key={id} className={`nav-item ${active===id?'active':''}`} onClick={()=>onNavigate(id)}><span className="nav-icon">{icon}</span>{label}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item"><span className="nav-icon">⚙</span>Cài đặt</button><div className="profile"><div className="avatar">{(user?.email||'AD').slice(0,2).toUpperCase()}</div><div><strong>{user?.fullName||user?.email||'Admin User'}</strong><small>Quản trị viên</small></div><button className="logout-button" onClick={logout} title="Đăng xuất">↪</button></div></div></aside><main className="main-content"><header className="topbar"><div className="crumb">Workspace <span>/</span> <b>{navItems.find(([id])=>id===active)?.[2]}</b></div><div className="top-actions"><button className="icon-button">⌕</button><button className="icon-button notification">♧<i/></button><div className="avatar small">{(user?.email||'AD').slice(0,2).toUpperCase()}</div></div></header>{children}</main></div>}
